<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= customer.customer_name %> - SKD Systems</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif; background: #f8fafc; }
        .navbar { position: sticky; top: 0; z-index: 40; background: rgba(255, 255, 255, 0.7); backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px); padding: 12px 0; border-bottom: 1px solid #e2e8f0; }
        .navbar-content { max-width: 1200px; margin: 0 auto; padding: 0 24px; display: flex; justify-content: space-between; align-items: center; }
        .navbar h1 { font-size: 20px; font-weight: 600; letter-spacing: -0.025em; color: #0f172a; }
        .gradient-text { background: linear-gradient(90deg, #E6282A, #FFA3A5); -webkit-background-clip: text; background-clip: text; color: transparent; }
        .back-link { color: #0f172a; text-decoration: none; font-weight: 500; padding: 8px 16px; border-radius: 8px; transition: all 0.2s; font-size: 14px; }
        .back-link:hover { background: #f1f5f9; }
        .container { max-width: 1200px; margin: 32px auto; padding: 0 24px; }
        .customer-header { background: white; padding: 30px; border-radius: 16px; box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1); border: 1px solid #e2e8f0; margin-bottom: 24px; }
        .customer-header h2 { color: #0f172a; font-size: 28px; font-weight: 600; letter-spacing: -0.025em; margin-bottom: 8px; }
        .customer-header .company { color: #64748b; font-size: 16px; }
        .metrics-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px; margin-bottom: 24px; }
        .metric-card { background: white; padding: 20px; border-radius: 16px; box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1); border: 1px solid #e2e8f0; }
        .metric-card h3 { color: #64748b; font-size: 13px; font-weight: 500; margin-bottom: 8px; }
        .metric-card .value { font-size: 24px; font-weight: 600; color: #0f172a; letter-spacing: -0.025em; }
        .section { background: white; padding: 30px; border-radius: 16px; box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1); border: 1px solid #e2e8f0; margin-bottom: 24px; }
        .section h3 { color: #0f172a; font-size: 20px; font-weight: 600; letter-spacing: -0.025em; margin-bottom: 20px; }
        .activity-item { padding: 16px; border-bottom: 1px solid #f1f5f9; }
        .activity-item:last-child { border-bottom: none; }
        .activity-header { display: flex; justify-content: space-between; margin-bottom: 8px; }
        .activity-type { font-weight: 600; color: #0f172a; }
        .activity-hours { color: #E6282A; font-weight: 600; }
        .activity-description { color: #64748b; font-size: 14px; margin-bottom: 5px; }
        .activity-date { color: #94a3b8; font-size: 12px; }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="navbar-content">
            <h1><span class="gradient-text">SKD</span>Systems Dashboard</h1>
            <a href="/dashboard" class="back-link">← Back to Dashboard</a>
        </div>
    </nav>

    <div class="container">
        <div class="customer-header">
            <h2><%= customer.customer_name %></h2>
            <p class="company"><%= customer.company_name || 'No company specified' %></p>
            <p class="company"><%= customer.email %></p>
        </div>

        <div class="metrics-grid">
            <div class="metric-card">
                <h3>Hours Purchased</h3>
                <div class="value"><%= parseFloat(customer.hours_purchased).toFixed(1) %>h</div>
            </div>
            <div class="metric-card">
                <h3>Hours Used</h3>
                <div class="value"><%= parseFloat(customer.hours_used).toFixed(1) %>h</div>
            </div>
            <div class="metric-card">
                <h3>Hours Remaining</h3>
                <div class="value"><%= parseFloat(customer.hours_remaining).toFixed(1) %>h</div>
            </div>
            <div class="metric-card">
                <h3>Usage</h3>
                <div class="value"><%= customer.usage_percentage || 0 %>%</div>
            </div>
        </div>

        <div class="section">
            <h3>Infrastructure Statistics</h3>
            <div class="metrics-grid">
                <div class="metric-card">
                    <h3>Server Count</h3>
                    <div class="value"><%= customer.server_count || 0 %></div>
                </div>
                <div class="metric-card">
                    <h3>Storage</h3>
                    <div class="value"><%= parseFloat(customer.storage_gb || 0).toFixed(1) %> GB</div>
                </div>
                <div class="metric-card">
                    <h3>Bandwidth</h3>
                    <div class="value"><%= parseFloat(customer.bandwidth_gb || 0).toFixed(1) %> GB</div>
                </div>
                <div class="metric-card">
                    <h3>Active Services</h3>
                    <div class="value"><%= customer.active_services || 0 %></div>
                </div>
            </div>
        </div>

        <div class="section">
            <h3>Recent Activity</h3>
            <% if (activities.length === 0) { %>
                <p style="color: #64748b;">No activity recorded yet.</p>
            <% } else { %>
                <% activities.forEach(activity => { %>
                    <div class="activity-item">
                        <div class="activity-header">
                            <span class="activity-type"><%= activity.activity_type %></span>
                            <span class="activity-hours"><%= parseFloat(activity.hours_consumed || 0).toFixed(1) %>h</span>
                        </div>
                        <div class="activity-description"><%= activity.description %></div>
                        <div class="activity-date">
                            <%= new Date(activity.activity_date).toLocaleDateString('en-US', { 
                                year: 'numeric', month: 'long', day: 'numeric', 
                                hour: '2-digit', minute: '2-digit' 
                            }) %>
                        </div>
                    </div>
                <% }); %>
            <% } %>
        </div>
    </div>
</body>
</html>